services:
  robot:
    build: .
    container_name: robot
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
      - ./.env.keys:/app/.env.keys:ro